# dependencies and build output
node_modules/
dist/

# the hub's state: database, intel snapshots, logs - never committed
data/
*.sqlite
*.sqlite-*
*.log
*.jsonl

# secrets - every key lives in hub/.env (copy hub/.env.example)
.env
.env.*
!.env.example

# local tooling and scratch
e2e/node_modules/
e2e/*.png
tradewarz-src.tgz
sync-in.cmd
_*.ps1
# brand images live in web/public; the root copies are working files
/avatar.png
/banner.png
.DS_Store
Thumbs.db
