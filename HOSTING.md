# Hosting

Production hosting for TradeWarz (API, indexer, leaderboard, and serving the built page) lives in
the private repository `tradewarz/tradewarz-server`, not in this public tree.

Ops: use that private repo's `render.yaml` / `HOSTING.md`. Do not point Render (or any deploy)
at this public repository - it no longer contains the server package.
